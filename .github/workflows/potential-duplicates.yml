name: 🆖　Potential Duplicates
on:
  issues:
    types: [opened, edited]
jobs:
  run:
    steps:
      - uses: bubkoo/potential-duplicates@v1
        with:
          GITHUB_TOKEN: ${{ secrets.bot_token }}

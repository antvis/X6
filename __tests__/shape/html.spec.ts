import { describe, expect, it } from 'vitest'

// import { HTML } from '../../src/shape/html'

describe('shape/html', () => {
  it('should have correct default attributes', () => {
    // expect(HTML).toBeDefined()
  })
})

{
  "private": true,
  "name": "x6",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna run clean",
    "build": "lerna run build",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "coveralls": "lerna run coveralls",
    "version": "lerna version",
    "clean:modules": "rimraf ./packages/*/node_modules && rimraf ./examples/*/node_modules && rimraf ./node_modules"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lerna run --concurrency 1 --stream precommit"
    }
  },
  "devDependencies": {
    "husky": "^3.1.0",
    "lerna": "^3.19.0",
    "rimraf": "^3.0.0"
  },
  "workspaces": [
    "examples/*",
    "packages/*"
  ]
}

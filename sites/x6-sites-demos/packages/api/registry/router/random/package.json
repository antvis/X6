{
  "name": "@antv/x6-sites-demos-api.registry.router.random",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@antv/x6": "^2.x",
    "@antv/x6-sites-demos-helper": "2.x",
    "antd": "^4.4.2",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-scripts": "^3.4.1"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "typescript": "^4.1.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "yarn -s prepare && yarn -s static",
    "prestart": "yarn prepare"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
